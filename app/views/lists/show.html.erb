  <% @bookmarks = Bookmark.all %>
<h1><%= @list.name %></h1>
<ul>
  <% @bookmarks.each do | bookmark | %>
  <li><%= bookmark.movie.title %> <%= link_to "delete", bookmark_path %></li>
  <%# (@bookmark), method: :delete, data: { confirm: 'Sure you wanna delete?'} %>
  <% end %>
</ul>


<%= link_to "Add a bookmark", new_list_bookmark_path(@list) %>  |
<%= link_to "Back to lists", lists_path %>
